회원관리 구현 - 실습
java > sevlet > jsp
jsp - 정적페이지 (html, css, js)와 동적페이지(java-서버사이드 결정됨)가 공존한다
-서버측에서 결정되고 그 결과가 화면에 다운로드 된다
회원목록보기, 조건검색 구현

select문
ResultSet - 커서를 조작할 수 있는 메소드를 제공해 주니까...
rs.next():boolean - ResultSet -> java api document문서를 보면....

forward로 이동하기
첫번째:요청이 유지되는 동안에.....
두번째:scope request 역할, 저장소
세번째:url은 바뀌지 않는데 출력 결과 페이지는 변했다
->a.jsp를 가리키고 있는데 실제 출력되는 페이지는 b.jsp이다

jdbc -> mjBatis API(ORM솔루션, SQL매핑오픈소스) -> MVC패턴반영 -> spring(mave, gradle)

action(서블릿 - 자바포함 -> MVC패턴) -> jsp

패턴화 시켜본다

int boolean{흐름을 바꿔줌 : 순서[알고리즘]가 달라질 수 있다} double
전변과 지변 차이점
원시형 변수 - 호출하면 값이 반환된다
참조형 변수 - 호출하면 주소번지가 반환된다. - 어렵다 - 5장 클래스 

회원가입 구현
jsp → action(insert) → action(select) → jsp
가입에 성공하면 1을 반환(오라클 서버에서) 하고 

int result = pstmt.executeUpdate();//요청을 한다

ResultSet rs = pstmt.executeQuery(); // 조회할 때
인터페이스이면서 추상메소드로 next와 같은 커서를 움직이는 메소드제공하는 것

MyBatis로 가면 이 코드들을 못보게 됨
왜냐하면 반복되는 코드를 줄여주는 컨셉이므로 생략하게 됨

ㅁ→ ||(MyBatis Layer)|| → DB(Oracle)
	이 계층에서
	쿼리문 : xml에 들어감
myBatis.jar lib에 추가해서 import해서 기능 사용 하는 프로세스
파라미터로 받아서코드를 줄 일 수 있음

----------------------------------
<input type="button">
과 <button type="button">
사이의 차이

button / input 차이
버튼 태그는 서브밋(클라이언트에서 서버로 전송)이 자동으로 일어남
인풋은 따로 서브밋을 해줘야 홈태그 안에 있는게 전송 됨(서브밋 메서드를 호출해줘야 홈태그 안에 있는 데이터들이 서버에 전송 됨)

"<button>버튼</button>"은 "<button type='submit'>버튼</button>"과 같습니다.

 

<button> 태그의 기본 타입이 "button" 이었으면 혼동이 훨씬 덜 했겠지만, 초창기 이 부분이 사용자들에게 불편함을 주면서 "<input type="button">"을 고수하게 만드는 한 가지 원인을 제공했습니다.

불필요한 폼 전송을 피하려면 "<button type='button'>버튼</button>" 과 같이 버튼 타입을 명시해서 버튼 태그를 사용해야 합니다.


────────────────────────────────────────
트러블 슈팅
1. 서블릿에서 if문을 제대로 타고 있는지?
else if("memebrInsert".equal(pMap.get("method").toString())) {
	//요기 경유하고 있니?
}
2. /membe/memberCRUD?method=memberInsert&mem_id=apple&mem_pw=123&mem_name=이순신
3. MemberDao클래스의 memberInsert() 경유했나? - 실제 오라클서버와 연동하기
:logger.info("memberInsert호출 성공");
4.SQLException인가 아니면 Exception인가?
:logger.info(se.toString()); //DML → executeUpdate()

원인1 - TNS Listener 어쩌구 저쩌구... 오라클 서버가 정상이 아니래
작업관리자(ctrl+alt+del 키) → 서비스 → OracleServiceORCL11 or ORacleServiceORCL12
XXXXXX11homeListener서비스 - 다시 시작 메뉴아이템 
Network Adapter 어쩌구 저쩌구 - 오라클서버가 정상이 아니래
만일 서버를 정상화 시킬 수 없다면 옆에 친구의 서버를 바라보고 테스트 하기

원인2
SELECT seq_member0518.nextval FROM dual; 에러
시쿼늣 이름에 오타가 있을 때 
PK는 not null
unique - 유일무이하다 
PK-FK 참조 무결성 제약조건 ......

원인3
INSERT INTO 테이블명(컬럼명1, 컬럼명2, 컬럼명3, ......) VALUES(?,?,?...)
?갯수만큼 치환되는 값이 있어야 해
pstmt.setString(1,"");

추신
get방식과 post방식
get방식의 경우에는 브라웢에게 인터셉트를 당할 수 있어요
이전에 한 번이라도 요청한 적이 있는 URL인 경우에 서버까지 전달하지 않고서
브라우저 캐쉬에 남아(기억)있는 페이지 정보를 그대로 내보내 준다
실제로 서버를 경유한 게 아니다.
동기화가 되어 있지 않다 - 의미 
:서로 맞춘다
:동기화가 되어 있어야 리얼이다
:현재의 정보이다
post방식은 데이터(파라미터)가 포장되어 있어서 인터셉트할 수 없다

------------------------------------------------------
String과 Stringbuilder
Stringbuilder는 "hello"에 대해서 다섯칸안에 한글자씩 넣음
잘라서 붙여넣기 가능 append로 추가 